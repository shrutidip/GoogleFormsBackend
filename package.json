{
  "name": "your-backend-app",
  "version": "1.0.0",
  "description": "Backend server for Windows Desktop Application",
  "main": "dist/server.js",  // Adjust based on your project structure
  "scripts": {
    "start": "node dist/server.js",  // Adjust as per your entry file
    "dev": "nodemon --watch src --exec ts-node src/server.ts",  // Development script using nodemon
    "build": "tsc -p ."  // TypeScript compilation script
  },
  "dependencies": {
    "express": "^4.17.2",
    // Other dependencies
  },
  "devDependencies": {
    "@types/express": "^4.17.12",
    "typescript": "^4.5.5",
    "ts-node": "^10.4.0",
    "nodemon": "^2.0.15"  // Development dependency for auto-restart
  }
}
